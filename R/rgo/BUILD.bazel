load("@com_grail_rules_r//R:defs.bzl", "r_library", "r_pkg")

r_pkg(
    name = "rgo",
    srcs = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ),
    cc_deps = [
        "//go/src/hello:hello.cc",
    ],
)

r_library(
    name = "library",
    pkgs = ["rgo"],
)
